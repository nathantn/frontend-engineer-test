import React from 'react';

import './Loading.scss';

const Loading = props => (
  <div className="loading">
    <div className="loading dot" />
    <div className="loading dot" />
    <div className="loading dot" />
  </div>
);

export default Loading;
